This is a utility command that can be executed from VS Tools menu to select a block within the tags <# and #>.
T4 and tt template files can be difficult to read with <# and #> tags. 
To help in distinguishing the parts of the template the selection of blocks acts as a highlighter.
The risk is deleting the selection accidentally.
A better way would be a nesting editor.
